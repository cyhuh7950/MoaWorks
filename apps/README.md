# Apps

이 디렉터리는 설계서 v1.2 반영 전 생성된 초안 흔적입니다.

## 현재 기준

- 권위 있는 저장소 구조는 `backend / frontend / deploy / docs` 입니다.
- 실제 구현 기준 위치는 `backend/` 와 `frontend/` 하위 디렉터리입니다.

## 사용 원칙

- 이 디렉터리의 내용은 참고용 이전 초안으로만 취급합니다.
- 새 구현은 이 경로를 기준으로 시작하지 않습니다.

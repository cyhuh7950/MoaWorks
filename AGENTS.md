# Aidot 작업 지침

## 1. 언어

- 모든 대화와 산출물은 한국어로 작성한다.

## 2. 작업 기준

- 코드의 단일 기준은 GitHub 저장소다.
- 대화, 기획, 요구사항, 문서의 기준은 ChatGPT Project다.
- 로컬 PC의 미커밋 상태는 공동 작업 기준점이 아니다.
- 다른 환경에서 이어서 작업해야 하면 반드시 commit 후 push 한다.

## 3. 도구별 역할

### PC(Codex 앱)

- 실제 코드 수정
- 로컬 실행
- 테스트 실행
- diff 최종 확인
- 병합
- 배포 전 점검

### Codex web

- 제한된 범위의 코드 수정
- 버그 원인 분석
- 테스트 실패 원인 파악
- PR 초안 작성
- 리뷰 보조

주의:
- 큰 구조 변경은 바로 Codex web에 맡기지 않는다.
- 수정 범위, 금지사항, 검토 항목을 먼저 정리한 뒤 작업시킨다.
- 가능하면 별도 브랜치 기준으로 작업한다.

### ChatGPT 채팅

- 설계
- 작업지시서 작성
- 영향 범위 분석
- 위험 요소 정리
- Codex용 프롬프트 정리
- PR diff 해석
- 회귀 가능성 검토
- 체크리스트 작성

### 모바일

- 현재 브랜치/PR 상태 확인
- 작업 지시
- 검토 및 확인
- 에러 로그 전달 및 원인 분석 요청

모바일에서 하지 않는 것:
- 대규모 구조 변경 승인
- 최종 병합 판단
- 운영 반영 결정

## 4. Git 브랜치 규칙

현재 기본 규칙은 아래와 같다.

- `main`: 운영 안정 브랜치
- `feature/*`: 기능 개발
- `fix/*`: 버그 수정
- `docs/*`: 문서 작업
- `refactor/*`: 꼭 필요한 경우에만 사용

규칙:
- `main` 직접 수정 금지
- 모든 작업은 기능 브랜치에서 시작
- 최종 병합은 PC에서만 수행
- 공동 작업에서는 한 작업 = 한 브랜치 원칙을 지킨다

프로젝트 규모가 커지면 아래 브랜치를 추가할 수 있다.

- `develop` 또는 `dev`: 통합 확인 브랜치

## 5. 커밋 및 동기화 규칙

- 의미 있는 단위로 자주 커밋한다.
- 모바일이나 다른 환경에서 이어서 볼 필요가 있으면 반드시 push 한다.
- 미커밋 상태를 오래 유지하지 않는다.

권장 흐름:

1. PC에서 작업
2. commit
3. push
4. Codex web / ChatGPT / 모바일에서 이어받기

## 6. 작업 요청 원칙

Codex web 또는 다른 에이전트에 작업을 맡길 때는 아래 정보를 포함한다.

- 작업 목적
- 수정 범위
- 금지사항
- 검토 항목
- 출력 방식

## 7. Codex web 작업지시 템플릿

```text
작업 목적:
- [해결할 문제]

수정 범위:
- [수정 가능한 범위]
- [건드리면 안 되는 범위]

검토 항목:
- 직접 영향 범위
- 깨질 수 있는 기존 기능
- 수정 파일 목록
- 변경 전/후 diff 요약

출력 방식:
1. 원인 요약
2. 수정 파일 목록
3. 예상 영향 범위
4. 수정 코드 또는 PR
```

## 8. ChatGPT 채팅 활용 원칙

ChatGPT 채팅은 직접 수정용보다 통제와 검토용으로 사용한다.

예시 요청:

- 이 이슈를 Codex에 맡기기 전에 영향 범위 분석해줘
- 아래 diff를 검토해서 회귀 위험만 정리해줘
- 모바일에서 Codex에 넣을 지시문으로 바꿔줘
- 이 PR이 설계서와 충돌하는지 확인해줘

## 9. 공동 작업 핵심 규칙

### 규칙 1. 한 작업 = 한 브랜치

- 여러 이슈를 한 브랜치에 섞지 않는다.

### 규칙 2. 큰 구조 변경은 먼저 설계/통제

- 먼저 ChatGPT 채팅에서 목적, 영향 범위, 금지사항을 정리한다.
- 그 다음 작업지시 프롬프트를 만들어 Codex에 전달한다.

### 규칙 3. 미커밋 상태로 환경 전환 금지

- 모바일이나 웹에서 이어서 보려면 push 된 상태여야 한다.

### 규칙 4. 최종 승인과 병합은 PC에서만

- 운영 영향 검토와 병합은 반드시 PC에서 수행한다.

## 10. 저장소 작업 우선순위

이 저장소에서는 아래 순서를 우선한다.

1. 설계 문서 정리
2. API/ERD/IA 확정
3. 스캐폴딩
4. 기능 구현
5. 테스트 및 검증

## 11. 참조 문서 우선 원칙

이 프로젝트의 기능/화면/운영 흐름은 아래 원본 문서를 우선 기준으로 삼는다.

- `docs/reference/original/[원본]Aidot 사용자 매뉴얼_v2.2.2.pdf`
- `docs/reference/original/[원본]Aidot NLU 활용 가이드_v2.2.2.pdf`

작업 시 반드시 아래 원칙을 따른다.

- 문서에 없는 기능은 임의로 추가하지 않는다.
- 기존 동작을 암묵적으로 변경하지 않는다.
- 화면 흐름과 운영 흐름을 우선한다.
- 불확실한 내용은 추측이라고 명시한다.
- 수정 범위와 영향 범위를 먼저 제시한다.
- 1.0 단계에서는 원본 매뉴얼과 최대한 비슷하게 구현한다.
- 화면/기능 재해석, 구조 개선, UX 고도화는 2.0 후보로만 문서화하고 1.0 구현에는 바로 반영하지 않는다.

문서에 영문 별칭 경로나 다른 이름이 등장하더라도 실제 저장소에 있는 원본 PDF를 기준으로 판단한다.

## 12. 로컬 재현성 원칙

- `.env.example` 유지
- `README.md` 최신화
- 실행/설치 방법 문서화
- 가능하면 의존성 lock 파일 유지
- 필요 시 `docker-compose.yml` 또는 실행 스크립트 추가

## 13. 작업 완료 보고 원칙

작업을 완료하면 항상 공유 가능한 형태의 작업 요약을 함께 제공한다.

권장 형식:

1. 작업 목적
2. 변경 내용 요약
3. 수정/추가 파일
4. 영향 범위
5. 실행 또는 확인 방법
6. 다음 작업

이 요약은 다른 팀원에게 바로 전달할 수 있는 수준으로 간결하고 명확해야 한다.

## 14. MoaWorks Subagent 파일럿 운영 계약

- 별도 Skill을 만들지 않고 `.agents/moaworks-subagent/main-instruction.md`를 메인 대화의 운영 지침으로 사용한다.
- 어울1은 설계 책임자이며 구현 종료까지 설계와 기술 판단을 소유한다.
- 어울2는 개발 Subagent이며 승인된 설계서, 작업지시서, 작업계획 전체를 전달받아 구현한다.
- Subagent 실행과 결과 판정은 `.agents/moaworks-subagent/README.md`와 `scripts/moaworks-subagent-state.mjs`의 상태 계약을 따른다.
- 같은 문제의 유효한 정식 실패 1~2회는 동일 개발자에게 갱신된 지시로 재작업한다.
- 같은 문제의 유효한 정식 실패 3회째에는 개발자를 중지하고 어울1이 직접 구현한다.
- 예기치 않은 중단은 실패 횟수에 포함하지 않고 같은 개발 작업을 이어서 수행한다.
- 어울1이 직접 구현하기 전에는 어울1과 어울2가 제품 코드를 동시에 수정하지 않는다.
- 기능 범위, 요구사항, 중요 위험이 바뀔 때만 신산님 승인을 요청한다.
- CLAUDE 검증은 이 파일럿과 결합하지 않고 기존 독립 절차를 유지한다.

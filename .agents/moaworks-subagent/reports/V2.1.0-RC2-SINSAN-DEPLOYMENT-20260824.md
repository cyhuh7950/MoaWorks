# v2.1.0-rc.2 sinsan 배포 보고서

## 판정

`DEPLOYED / AUTHENTICATED_OPERATIONAL_VERIFICATION_PENDING`

승인된 서비스 배포와 비파괴 검증은 완료했다. 인증 사용자·관리자 기능, 실제 개인 AI Provider, Android 실기기 검증은 아직 완료 증거가 아니므로 전체 완료로 판정하지 않는다.

## 릴리스 기준

- Git tag: `v2.1.0-rc.2`
- Git revision: `dd2c0a2c288b55b5f12dc11b8369e77d94b0ea2d`
- package version: admin/user/mobile 모두 `0.1.0`
- 운영 checkout: detached `v2.1.0-rc.2`, 사용자 소유 미추적 `" "`, `backups/` 보존

## 사전 보호

- DB custom dump: `/home/ubuntu/deploy/moaworks-backups/v2.1.0-rc1-20260824-2f48d53/moaworks.dump`
- size: `2,451,640 bytes`
- SHA-256: `aa4b38d46602997c27f5bb62682f0027a08a23a341c4cf429a57ba3dc9839f1b`
- `sha256sum -c`: PASS
- `pg_restore --list`: 678 entries
- 배포 전 server/admin/user/worker image에 `rollback-v2.1.0-rc1-pre` 태그 보존
- DB·mail spool·storage·ClamAV 볼륨 삭제 또는 restore 미실행

## 배포 결과

- migration: `063_personal_ai.sql` 적용 및 `schema_migrations` 기록 PASS
- 신규 테이블: `personal_ai_configs`, `personal_ai_rate_limits` 2개 확인
- 기존 개인 AI 데이터: config 0건, rate limit 0건
- 대상 서비스: server, mail-layer, mail-external-worker, mail-backup-worker, mail-retention-worker, admin-web, user-web
- 제외 서비스: PostgreSQL, storage, mail-gateway, rspamd, ClamAV
- restart count: 대상 7개 모두 0

## 이미지 digest

- server 및 worker 4종: `sha256:a97aeb6f64f930fb256544af62561fc37f2d45d75deddb18b3a2b0ea34dae935`
- admin-web: `sha256:24b9f708df8270a626560ee3fbe972484243b01a631e486c3abf3c5fed22f735`
- user-web: `sha256:4c3b759325830f69e6f89272f20433bdeec3acfff1e9d6b4602627be4e8d0fc3`
- 각 image에 `v2.1.0-rc.2` 태그를 추가했다.

## 검증

- 내부 `/api/v1/health`: app/db/mail/storage 모두 `ok`
- 공개 API health: HTTP 200
- user HTTPS: HTTP 200
- admin HTTPS: HTTP 200
- admin same-origin 공개 UI 계약: HTTP 200
- 개인 AI config 무인증 요청: HTTP 401
- 운영 repo와 server container의 `directory_store.py` SHA-256 일치: `2eb7c34ecab8af5a6fc47d871ebc5a499b43cadee48e22f06353935d213347ac`
- admin bundle에 명시적 초기 비밀번호 안내 포함, 이전 아이디 동일 비밀번호 문구 없음
- 운영 `SETUP_SECRET_KEY`: 기본값 아님. 실제 값은 출력하지 않았다.

## 배포 중 오류와 복구

1. `v2.1.0-rc.1` admin build에서 공개 UI 계약 state/import와 초기 비밀번호 계약의 병합 회귀 12건을 발견했다. 실행 컨테이너 교체 전에 중단했다.
2. clean audit에서 `nanoid 3.3.17` high 1건을 발견해 승인 후 `3.3.18` patch로 audit 0을 확인했다.
3. 독립 리뷰에서 `update_user` SQL placeholder/binding 불일치와 중복 부서장 필드를 발견해 승인 후 RED/GREEN·전체 583건·재리뷰 0/0/0으로 교정했다.
4. 로컬 annotated tag 생성은 `.git` ACL로 1회 실패했고 승인된 권한으로 동일 명령을 재실행했다.
5. 개인 AI 무인증 확인의 최초 URL을 `/personal-ai/config`로 잘못 호출해 404를 받았고 router의 실제 `/workspace/personal-ai/config`로 재확인해 401을 확인했다.

같은 근본 원인이 3회 연속 반복된 사례는 없다.

## 미검증

- 인증된 사용자·관리자 운영 UI/API/DB/감사
- 개인 AI 사용자별 설정 저장·암호화 행·실제 Provider 연결·채팅
- 운영 암호화 키 rotation
- Android 실기기·TalkBack, iOS/macOS/Xcode/VoiceOver
- 실제 외부 메일/첨부/전달과 사용자 수정 대장 잔여 상호작용
- 별도 신규 설치 담당자의 독립 설치 재현
- `deploy` compose의 종료된 legacy storage 항목 정리. 실제 `moaworks-storage`와 health는 정상이며 승인 없이 변경하지 않았다.

## 다음 조치

1. 신산님이 전용 테스트 사용자로 Chrome에 로그인한다.
2. 민감 작업 직전에 별도 확인을 받아 인증 기능과 사용자 수정 대장을 검증한다.
3. 개인 AI Provider 키는 화면에서 신산님이 직접 입력하고 실제 비용 호출을 승인한 뒤 검증한다.
4. Android 실기기를 USB로 연결해 release APK 설치와 핵심 기능을 검증한다.
